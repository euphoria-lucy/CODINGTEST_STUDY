#include <stdio.h>

int main() {
	char*a[3] = {"kor", "eng", "수학"};
	char b[4][7] = {"kor", "eng", "수학"};
	
	int i;
	// for (i = 0; i <= 2; i++0 {
	// printf("a[%d] = %s, %x\n", i, a[i], *(a + i));
	// printf("%s %s %s", b[i];
	// }
	printf("%s %s %s\n\n", b[0], b[1], b[2]);
	printf("%d %d %d", b[0], b[1], b[2]);
}
